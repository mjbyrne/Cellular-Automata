#include <iostream>
#include <vector>
using namespace std;


int boardCreate {
  int board[100][100];
  int a = 0;
  
  for (int y = 0; y < 100; ++y){
    for(int x = 0; x < 100; ++x){
      a = rand() %2;
      board[i][j] = a; 
    }
  }
}

int neighborCount {
  int aliveNeighbors = 0;
  int neighbors[100][100];
  int neighborXPos = 0;
  int neighborYPos = 0;
  
  for (int y = 0; y < 100; ++y){
    for(int x = 0; x < 100; ++x){
      for(int neighborNumber = 0; neighborNumber < 8; ++neighborNumber){
        //above and left
        if (neighborNumber == 0){ 
          if (y==0){
            neighborYPos = 99;
          }
          else {
            neighborYPos = y-1;
          }
          if (x==0){
            neighborXPos = 99;
          }
          else{
            neighborXPos = x-1;
          }
          neighbors[y][x] = neighbors[y][x] + board[neighborYPos][neighborXPos];
        }
        
        //above  
        if (neighborNumber == 1){
          if(y==0){
            neighborYPos = 99;
          }
          else {
            neighborYPos = y-1;
          }
          neighborXPos = x;
        }
        
        //above and right
        if (neighborNumber == 2){
          if(y==99){
            neighborYPos = 0
          }
          else{
            neighborYPos = y+1;
          }
          if(x(==99){
            neighborXPos = 0;
          }
          else {
            neighborXPos = x+1;
          }
          neighbors[y][x] = board{neighborYPos][neighborXPos];
        }
        
        
        // left
        if (neighborNumber == 3){
          
        }
        
        // right
        if (neighborNumber == 4){
          
        }
        
        //below and left
        if (neighborNumber == 5){
          
        }
        
        // below
        if (neighborNumber == 6){
          
        }
        
        // below and right
        if (neighborNumber == 7){
          
        }
        
      }
    }
  }
}



int main() {
   vector<vector<char>> boardVals;
   // 15x15 board
   
   for(int y = 0; y <= 25; y++){
     vector<char> temp;
     for(int x = 0; x <=75; x++){
       temp.push_back('O'); 
      }
      boardVals.push_back(temp);
   }
   
   for(int y = 0; y <= 25; y++){
     for(int x = 0; x <=75; x++){
       cout << boardVals[y][x];
     }
     cout << endl;
   }
   
   
   
}
/*  RULES
    alive cell if neighbors < 2        cell dies
    alive cell if 1 < neighbors < 4    cell stays alive
    alive cell if neighbors > 3        cell dies
    alive cell if neighbors  ==3       cell becomes alive
*/
