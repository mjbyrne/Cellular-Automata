#include <iostream>
#include <vector>
using namespace std;


int boardCreate {
  int board[100][100];
  int a = 0;
  
  for (int y = 0; y < 100; ++y){
    for(int x = 0; x < 100; ++x){
      a = rand() %2;
      board[i][j] = a; 
    }
  }
}

int neighborCount {
  int neighbors[100][100];
  for (int y = 0; y < 100; ++y){
    for(int x = 0; x < 100; ++x){
      
    }
  }
}



int main() {
   vector<vector<char>> boardVals;
   // 15x15 board
   
   for(int y = 0; y <= 25; y++){
     vector<char> temp;
     for(int x = 0; x <=75; x++){
       temp.push_back('O'); 
      }
      boardVals.push_back(temp);
   }
   
   for(int y = 0; y <= 25; y++){
     for(int x = 0; x <=75; x++){
       cout << boardVals[y][x];
     }
     cout << endl;
   }
   
   
   
}
/*  RULES
    alive cell if neighbors < 2        cell dies
    alive cell if 1 < neighbors < 4    cell stays alive
    alive cell if neighbors > 3        cell dies
    alive cell if neighbors  ==3       cell becomes alive
*/
